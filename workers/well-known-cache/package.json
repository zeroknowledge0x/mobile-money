{
  "name": "well-known-cache-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for caching .well-known endpoints",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  }
}
