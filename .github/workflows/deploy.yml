name: Deploy

on:
  push:
    branches: [main]

env:
  IMAGE: ${{ github.repository }}
  DEPLOY_TIMEOUT: "120s"
  HEALTHCHECK_RETRIES: "10"

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check if deployment is configured
        id: check_deploy
        run: |
          if [ -n "${{ secrets.REGISTRY_USERNAME }}" ] && [ -n "${{ secrets.REGISTRY_PASSWORD }}" ] && [ -n "${{ secrets.KUBECONFIG }}" ]; then
            echo "deploy_available=true" >> $GITHUB_OUTPUT
          else
            echo "deploy_available=false" >> $GITHUB_OUTPUT
            echo "⚠️ Deployment secrets not configured. Skipping deployment."
            echo "To enable deployment, add REGISTRY_USERNAME, REGISTRY_PASSWORD, and KUBECONFIG secrets."
            exit 0
          fi

      - name: Set up Docker Buildx
        if: steps.check_deploy.outputs.deploy_available == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Log in to registry
        if: steps.check_deploy.outputs.deploy_available == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build & push image
        if: steps.check_deploy.outputs.deploy_available == 'true'
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ github.sha }}
            ${{ env.IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ── Kubernetes deploy ──────────────────────────────────────────────────
      - name: Configure kubectl
        if: steps.check_deploy.outputs.deploy_available == 'true'
        uses: azure/k8s-set-context@v3
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBECONFIG }}

      - name: Apply manifests
        if: steps.check_deploy.outputs.deploy_available == 'true'
        run: kubectl apply -f k8s/

      - name: Update image
        if: steps.check_deploy.outputs.deploy_available == 'true'
        run: |
          kubectl set image deployment/mobile-money \
            mobile-money=${{ env.IMAGE }}:${{ github.sha }}

      # ── Wait for rollout ───────────────────────────────────────────────────
      - name: Wait for rollout
        if: steps.check_deploy.outputs.deploy_available == 'true'
        id: rollout
        run: |
          kubectl rollout status deployment/mobile-money \
            --timeout=${{ env.DEPLOY_TIMEOUT }}
        # Don't fail the step yet — we check health next

      # ── Healthcheck against /ready ─────────────────────────────────────────
      - name: Healthcheck
        id: healthcheck
        if: steps.check_deploy.outputs.deploy_available == 'true' && steps.rollout.outcome == 'success'
        run: |
          ENDPOINT="${{ secrets.APP_HEALTHCHECK_URL }}/ready"
          for i in $(seq 1 ${{ env.HEALTHCHECK_RETRIES }}); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$ENDPOINT" || echo "000")
            echo "Attempt $i: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then
              echo "Healthcheck passed"
              exit 0
            fi
            sleep 6
          done
          echo "Healthcheck failed after ${{ env.HEALTHCHECK_RETRIES }} attempts"
          exit 1

      # ── Automated rollback ─────────────────────────────────────────────────
      - name: Rollback on failure
        if: steps.check_deploy.outputs.deploy_available == 'true' && failure() && (steps.rollout.outcome == 'failure' || steps.healthcheck.outcome == 'failure')
        run: |
          echo "::warning::Deployment failed — rolling back to previous revision"
          kubectl rollout undo deployment/mobile-money
          kubectl rollout status deployment/mobile-money --timeout=${{ env.DEPLOY_TIMEOUT }}

      - name: Check if Slack is configured
        if: steps.check_deploy.outputs.deploy_available == 'true' && failure() && (steps.rollout.outcome == 'failure' || steps.healthcheck.outcome == 'failure')
        id: check_slack
        run: |
          if [ -n "${{ secrets.SLACK_WEBHOOK_URL }}" ]; then
            echo "slack_available=true" >> $GITHUB_OUTPUT
          else
            echo "slack_available=false" >> $GITHUB_OUTPUT
          fi

      - name: Notify on rollback
        if: steps.check_deploy.outputs.deploy_available == 'true' && steps.check_slack.outputs.slack_available == 'true' && failure() && (steps.rollout.outcome == 'failure' || steps.healthcheck.outcome == 'failure')
        uses: slackapi/slack-github-action@v1
        with:
          payload: |
            {
              "text": ":rotating_light: *Deployment rolled back* — `${{ github.sha }}` failed healthcheck on `mobile-money`. Previous version restored."
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        continue-on-error: true
